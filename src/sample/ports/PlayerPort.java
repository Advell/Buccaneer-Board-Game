package sample.ports;

import sample.InGameColor;
import sample.ports.Port;

public class PlayerPort extends Port {

    public PlayerPort(InGameColor color, String portName) {
        super(color, portName);
    }

}
